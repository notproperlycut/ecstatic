context/auth? -> typed_struct -> tests
update

Aggregate lifespans, snapshots
authz into Ecstatic
JSON schema strings
non_null and eventual consistency, system definitions

force_ssl
api secret in DB - encrypt or remove

auth access to application systems, components, etc
query {application(id: "not an id"){applicationId}}

args, filtering, pagination
mutation payloads - status and errors
